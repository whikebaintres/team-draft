# 팀 드래프트 — 가챠 드래프트 & 관리자 점수 등록

e스포츠 동호회용 랜덤 팀 뽑기 시스템. `gacha.html`(드래프트)과 `admin.html`(관리자 점수 등록) 두 페이지가 하나의 GitHub 저장소(`gacha-state.json`)를 공유 데이터스토어로 써서 서로 연결되어 있다.

> 이 레포에는 이전에 만든 핀볼 방식 드래프트(`index.html` / `state.json`)도 함께 있지만, 이 문서는 이후에 추가된 가챠 드래프트 시스템(`gacha.html`, `admin.html`, `gacha-state.json`)만 다룬다. 두 시스템은 서로 다른 파일을 쓰기 때문에 충돌하지 않는다.

## 전체 흐름

1. **`gacha.html`** 에서 참가자 명단을 관리하고 팀 수를 정한 뒤 드래프트를 실행한다.
2. 카드가 한 명씩 등장하며 팀에 배정되고, 결과가 나오면 (호스트 토큰이 있을 경우) `gacha-state.json`에 이번 드래프트 결과가 공유된다.
3. **`admin.html`** 에서 그 결과를 조회하고, 실제 경기 후 각 참가자의 승/패/무와 POM을 등록한다.
4. 등록된 점수는 `gacha-state.json`의 누적 전적(`playerStats`, `headToHead`)에 반영되고, 다음에 `gacha.html`을 다시 열면 카드에 갱신된 수치가 보인다.

## 페이지별 기능

### `gacha.html` — 드래프트 페이지

- **참가자 관리**: 추가/삭제 (기본 11명), 변경된 항목만 애니메이션 (전체 리스트 깜빡임 없음)
- **팀 구성**: 최소 2팀 ~ 최대 8팀, 팀 이름은 국내 e스포츠 구단명 중 랜덤 배정
- **기록 모드 스위치** (팀 구성 패널 상단): 이번 달 년월 라벨(예: "2026년 7월", 매달 자동 갱신) 또는 "테스트" 중 선택
  - **년월(정식) 모드**: 드래프트가 끝나면 결과가 `gacha-state.json`에 공유되어 관리자 페이지에서 채점 가능
  - **테스트 모드**: 결과가 전혀 공유되지 않음 (GitHub에 아무 요청도 안 감) — 연습/테스트용
  - "새로 시작하기"를 누르면 항상 정식 모드로 자동 리셋됨 (테스트 모드가 실수로 이어지는 것 방지)
- **드래프트 연출** (철권 VS 화면 참고):
  - 왼쪽 선수 카드 등장 → **3초** 대기 → 상대(오른쪽) 카드 등장 → 약 0.9초 후 VS 마크 + 포지션 라벨 + 상대전적 + 해설 문구 동시 공개 → 5초 대기 → 다음 페어로 전환
  - 포지션 라벨: **선봉전 → 차봉전 → 중견전 → 부장전 → 대장전** (전체 5쌍일 때). 정확히 4쌍이면 중견전을 빼고 선봉전·차봉전·부장전·대장전만 사용
  - 카드: 방패형 실루엣 카드(사람 아이콘 자리에 나중에 사진 교체 가능), 팀 컬러 글로우, 개인 승/패/승률/POM 표시
  - **상대전적**: 이번 매치업 두 사람의 과거 전적을 "OOO 2승 0패로 우위" 처럼 앞서는 사람 이름을 명시해서 표시, 첫 대결이면 "첫 맞대결"
  - **해설 문구**: 상대전적 상황(첫 대결/막상막하/압도적 우위)에 따라 다양한 문구가 랜덤하게 노출
- **BGM**: "드래프트 시작하기" 클릭 시 지정된 유튜브 영상이 배경음악으로 재생 (숨김 플레이어). 우측 상단 스피커 아이콘 클릭으로 음소거, 호버 시 세로 볼륨 슬라이더 노출
- **공유 설정**: GitHub 토큰(쓰기 권한)을 넣으면 결과가 자동 공유됨. 토큰이 없으면 그 브라우저에서만 진행 (공유 안 됨)

### `admin.html` — 관리자 점수 등록 페이지

- 최근 드래프트 결과(팀별 명단, 어느 년월인지)를 조회
- 참가자별로 승/패/무 라디오 선택 (개인별 기록은 참고용이라 전원 다 채울 필요 없음, 부분 입력 허용)
- 전체 참가자 중 POM 1명 선택 ("POM 없음" 옵션 포함)
- 등록하면 `playerStats`(개인 누적 승/패/무/POM)와 `headToHead`(매치업 쌍별 상대전적)가 함께 갱신됨
- 이미 채점된 결과는 읽기 전용으로 보여주고 "다시 채점하기"로 재오픈 가능 (재채점 시 이전 기여분을 먼저 되돌린 뒤 새로 반영)
- 관리자 토큰(쓰기 권한) 입력 필요 — 없으면 조회만 가능, 등록은 불가

## 데이터 모델 — `gacha-state.json`

```json
{
  "playerStats": {
    "고재형": { "win": 12, "loss": 6, "draw": 0, "pom": 2 }
  },
  "headToHead": {
    "고재형|지한규": { "a": "고재형", "b": "지한규", "aWin": 2, "bWin": 0, "draw": 0 }
  },
  "latestDraft": {
    "runId": "d...",
    "createdAt": 1730000000000,
    "yearMonth": "2026년 7월",
    "scored": false,
    "teams": [{ "name": "T1", "color": "#0a5cd8", "colorName": "딥 블루", "members": ["..."] }],
    "pairs": [["이름A", "이름B"], ["이름C", "이름D"]],
    "results": null
  }
}
```

- `playerStats`: 참가자별 누적 전적. 최초 시드는 `asset/DATABASE.xlsx`(과거 A팀/B팀 대결 기록, POM은 쉼표로 구분된 이름 목록)를 파싱해서 만들었다.
- `headToHead`: 두 사람이 실제로 맞붙어서 나온 승/패/무 누적. 키는 두 이름을 `|`로 이은 것이며 순서는 상관없이 양방향으로 조회 가능하게 설계되어 있다.
- `latestDraft`: 가장 최근 드래프트 1건만 보관 (히스토리 큐는 없음). `pairs`는 각 대결이 실제로 누구와 누구였는지를 기록해, 관리자 점수 등록 시 `headToHead`를 자동으로 갱신하는 데 쓰인다.

## 공유 방식 (GitHub-as-datastore)

두 페이지 모두 별도 백엔드 없이, 이 저장소의 `gacha-state.json`을 GitHub Contents API로 읽고/쓴다.

- **읽기**: 인증 없이 상대경로로 `fetch('gacha-state.json')` — GitHub Pages로 호스팅되어야 동작한다 (`file://`로 직접 열면 브라우저가 fetch를 막아서 전적이 전부 0으로 보인다).
- **쓰기**: `repo` 쓰기 권한이 있는 GitHub 토큰이 필요. 각 페이지의 토큰 입력칸에 붙여넣으면 `localStorage`에 저장되어 다음에도 유지된다.
- 핀볼 페이지(`index.html`/`state.json`)가 쓰던 것과 동일한 패턴을 재사용했다.

## 알려진 제한 사항

- `latestDraft`는 한 건만 보관하므로, 채점하기 전에 새 드래프트를 돌리면 이전 미채점 결과는 덮어써진다.
- 팀 수가 2보다 크면 화면상의 "왼쪽 vs 오른쪽" 페어링이 실제 팀 간 대결과 정확히 일치하지 않을 수 있다 (연출/연속 등장 순서 기준이라 2팀일 때 가장 자연스럽다).
- 이 README가 다루는 기능은 로컬 정적 서버 또는 GitHub Pages 배포본에서 검증되었다. 로컬 파일 더블클릭(`file://`)으로는 전적 연동 기능이 동작하지 않는다.
